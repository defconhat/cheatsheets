# 🖥️ Терминал Linux — шпаргалка по командам

> Универсальная шпаргалка для bash/zsh/fish.
> Обозначения: `<file>` — обязательный аргумент, `[file]` — необязательный,
> `...` — повторяется. Большинство команд поддерживают `--help` и `man <cmd>`.

---

## 📁 Навигация по файловой системе

| Команда | Действие |
|---|---|
| `pwd` | Текущий путь (print working directory) |
| `ls` | Список файлов |
| `ls -la` | Подробный список + скрытые файлы |
| `ls -lh` | Размеры в читаемом виде (KB/MB) |
| `ls -lt` | По времени изменения |
| `ls -R` | Рекурсивно (с подкаталогами) |
| `cd <dir>` | Перейти в каталог |
| `cd ..` | На уровень выше |
| `cd ~` / `cd` | Домашний каталог |
| `cd -` | Вернуться в предыдущий каталог |
| `pushd <dir>` / `popd` | Перейти с запоминанием / вернуться |
| `tree` | Дерево каталогов (`tree -L 2` — глубина 2) |
| `find <dir> -name "*.py"` | Найти файлы по имени |
| `fd "pattern"` | Быстрый поиск (нужен `fd` / `fd-find`) |

---

## 📄 Работа с файлами и каталогами

| Команда | Действие |
|---|---|
| `touch <file>` | Создать пустой файл / обновить время |
| `mkdir <dir>` | Создать каталог |
| `mkdir -p a/b/c` | Создать с родителями (без ошибок) |
| `cp <src> <dst>` | Копировать |
| `cp -r <src> <dst>` | Копировать рекурсивно (каталог) |
| `mv <src> <dst>` | Переместить / переименовать |
| `rm <file>` | Удалить файл |
| `rm -r <dir>` | Удалить каталог рекурсивно |
| `rm -rf <dir>` | Удалить принудительно (⚠️ безвозвратно) |
| `ln -s <target> <link>` | Создать символическую ссылку |
| `stat <file>` | Подробная информация о файле |
| `file <file>` | Определить тип файла |

---

## 📖 Просмотр содержимого файлов

| Команда | Действие |
|---|---|
| `cat <file>` | Вывести весь файл |
| `less <file>` | Постраничный просмотр (`q` — выход, `/` — поиск) |
| `more <file>` | Простейший постраничный просмотр |
| `head -n 20 <file>` | Первые 20 строк |
| `tail -n 20 <file>` | Последние 20 строк |
| `tail -f <file>` | Следить за обновлениями (логи) |
| `bat <file>` | `cat` с подсветкой синтаксиса (если установлен) |
| `wc -l <file>` | Подсчёт строк/слов/байт |
| `grep "text" <file>` | Поиск текста в файле |

---

## 🔍 Поиск

| Команда | Действие |
|---|---|
| `grep "pattern" <file>` | Поиск строк по шаблону |
| `grep -r "pattern" <dir>` | Рекурсивно |
| `grep -i "pattern"` | Без учёта регистра |
| `grep -n "pattern"` | С номерами строк |
| `grep -v "pattern"` | Инверсия (НЕ совпадает) |
| `grep -E "a\|b"` | Регулярные выражения (= `egrep`) |
| `ripgrep "pattern" <dir>` / `rg "pattern"` | Очень быстрый поиск |
| `find <dir> -name "*.py"` | Поиск файлов по имени |
| `find <dir> -type f -size +10M` | Файлы больше 10 МБ |
| `find <dir> -mtime -1` | Изменённые за последние сутки |
| `locate <name>` | Поиск по индексу (`updatedb`) |

---

## 📦 Архивы

| Команда | Действие |
|---|---|
| `tar -cvf archive.tar <dir>` | Создать `.tar` |
| `tar -xvf archive.tar` | Распаковать `.tar` |
| `tar -czvf archive.tar.gz <dir>` | Создать `.tar.gz` (gzip) |
| `tar -xzvf archive.tar.gz` | Распаковать `.tar.gz` |
| `tar -cjvf archive.tar.bz2 <dir>` | Создать `.tar.bz2` (bzip2) |
| `tar -xjvf archive.tar.bz2` | Распаковать `.tar.bz2` |
| `gzip <file>` / `gunzip <file>` | Сжать / распаковать `.gz` |
| `zip -r archive.zip <dir>` | Создать `.zip` |
| `unzip archive.zip` | Распаковать `.zip` |
| `7z a archive.7z <dir>` | Создать `.7z` (нужен `p7zip`) |
| `7z x archive.7z` | Распаковать `.7z` |
| ` unrar x archive.rar` | Распаковать `.rar` |

Мнемоника для tar-флагов: **c**reate / e**x**tract, **v**erbose, **f**ile,
**z** = gzip, **j** = bzip2.

---

## ⚙️ Процессы

| Команда | Действие |
|---|---|
| `ps aux` | Все процессы |
| `ps aux \| grep <name>` | Найти процесс по имени |
| `top` / `htop` / `btop` | Интерактивный мониторинг |
| `kill <pid>` | Послать SIGTERM |
| `kill -9 <pid>` | Принудительно завершить (SIGKILL) |
| `killall <name>` | Завершить все процессы по имени |
| `pkill -f <pattern>` | Убить по шаблону командной строки |
| `jobs` | Фоновые задачи текущего shell |
| `fg` / `bg` | Перенести на前台 / в фон |
| `Ctrl-Z` | Приостановить процесс |
| `nohup <cmd> &` | Запустить, не зависящий от hangup |
| `<cmd> &` | Запустить в фоне |
| `disown` | Отвязать процесс от shell |
| `systemctl status <svc>` | Статус сервиса (systemd) |
| `journalctl -u <svc> -f` | Логи сервиса в реальном времени |

---

## 💾 Диск и память

| Команда | Действие |
|---|---|
| `df -h` | Свободное место на дисках |
| `du -sh <dir>` | Размер каталога |
| `du -sh * \| sort -h` | Размеры элементов каталога, отсортированные |
| `ncdu <dir>` | Интерактивный анализатор размера |
| `free -h` | Оперативная память |
| `htop` / `btop` | Память + CPU интерактивно |
| `lsblk` | Блочные устройства |
| `mount` / `umount` | Монтирование / отмонтирование |
| `findmnt` | Дерево смонтированных ФС |
| `iostat` / `iotop` | Нагрузка на ввод-вывод |

---

## 🌐 Сеть

| Команда | Действие |
|---|---|
| `ip a` / `ip addr` | Сетевые интерфейсы и IP |
| `ip r` / `ip route` | Маршруты |
| `ping <host>` | Проверка связи |
| `curl <url>` | HTTP-запрос |
| `curl -O <url>` | Скачать файл |
| `curl -fsSL <url> \| sh` | Скачать и запустить скрипт |
| `wget <url>` | Скачать файл |
| `wget -c <url>` | Continue (докачка) |
| `ss -tlnp` | Открытые TCP-порты и процессы |
| `ss -tuln` | Все прослушиваемые порты |
| `netstat -tulpn` | То же (старый вариант) |
| `dig <domain>` / `nslookup` | DNS-запрос |
| `ssh user@host` | Подключение по SSH |
| `scp file user@host:/path` | Копирование по SSH |
| `rsync -avz src/ user@host:dst/` | Синхронизация по SSH |

---

## 👤 Пользователи и права

| Команда | Действие |
|---|---|
| `whoami` | Текущий пользователь |
| `id` | UID/GID и группы |
| `who` / `w` | Кто залогинен |
| `chmod 755 <file>` | Изменить права (цифрами) |
| `chmod +x <file>` | Сделать исполняемым |
| `chmod u+x,g+w <file>` | Точные права |
| `chown user:group <file>` | Сменить владельца |
| `chown -R user:group <dir>` | Рекурсивно |
| `sudo <cmd>` | Выполнить от root |
| `su -` | Стать root |
| `passwd` | Сменить пароль |
| `useradd` / `usermod` / `userdel` | Управление пользователями |
| `groupadd` / `gpasswd` | Управление группами |

### Цифровые права chmod
```
7 = rwx (read+write+exec)   6 = rw-   5 = r-x   4 = r--   0 = ---
owner / group / others
755 = rwxr-xr-x   644 = rw-r--r--   600 = rw-------
```

---

## 📦 Пакетные менеджеры

### Arch / CachyOS (pacman + yay)
```bash
sudo pacman -Syu              # обновить систему
sudo pacman -S <pkg>          # установить
sudo pacman -R <pkg>          # удалить
sudo pacman -Rns <pkg>        # удалить с зависимостями и конфигами
sudo pacman -Ss <name>        # поиск в репозитории
sudo pacman -Qe               # установленные явно пакеты
sudo pacman -Qdtq             # пакеты-сироты (для удаления)
yay -S <aur-pkg>              # установка из AUR
yay -Syu                      # обновить с AUR
paru -S <aur-pkg>             # paru — альтернатива yay
```

### Debian / Ubuntu (apt)
```bash
sudo apt update               # обновить список пакетов
sudo apt upgrade              # обновить пакеты
sudo apt install <pkg>        # установить
sudo apt remove <pkg>         # удалить
sudo apt autoremove           # удалить неиспользуемые зависимости
apt search <name>             # поиск
dpkg -L <pkg>                 # файлы пакета
```

### Fedora (dnf)
```bash
sudo dnf install <pkg>
sudo dnf upgrade
sudo dnf remove <pkg>
dnf search <name>
```

### Универсально (Flatpak / Snap)
```bash
flatpak install flathub <app>
flatpak update
flatpak list
snap install <pkg>
```

---

## 🔄 Перенаправления и пайпы

| Конструкция | Действие |
|---|---|
| `cmd > file` | Перенаправить stdout в файл (перезапись) |
| `cmd >> file` | Дописать в файл |
| `cmd 2> file` | Перенаправить stderr |
| `cmd > file 2>&1` | stdout + stderr в файл |
| `cmd < file` | Читать stdin из файла |
| `cmd1 \| cmd2` | Передать stdout первой во stdin второй |
| `cmd \| tee file` | Вывести и сохранить в файл одновременно |
| `cmd \| less` | Постраничный просмотр вывода |
| `cmd \| xargs <cmd>` | Передать вывод как аргументы |
| `cmd1 && cmd2` | cmd2 только если cmd1 успешна |
| `cmd1 \|\| cmd2` | cmd2 только если cmd1 неудачна |
| `` `cmd` `` или `$(cmd)` | Подстановка вывода команды |

---

## ✂️ Текстовые утилиты

| Команда | Действие |
|---|---|
| `sort <file>` | Отсортировать строки |
| `sort -h` | Человеко-читаемые числа (1K, 2M) |
| `sort -u` / `uniq` | Уникальные строки (после sort) |
| `uniq -c` | Подсчёт повторов |
| `cut -d: -f1 /etc/passwd` | Вырезать поле (разделитель `:`) |
| `tr 'a-z' 'A-Z'` | Трансформация символов |
| `sed 's/old/new/g' <file>` | Замена текста |
| `sed -i 's/old/new/g' <file>` | Замена в файле (in-place) |
| `awk '{print $1}'` | Печать первого поля каждой строки |
| `awk -F: '{print $1}' /etc/passwd` | С разделителем |
| `paste a b` | Объединить файлы по столбцам |
| `column -t <file>` | Выровнять в виде таблицы |
| `rev` | Развернуть строку задом наперёд |
| `jq '.field'` | Парсинг JSON |
| `jq '.[] \| .name'` | Извлечь массив имён |
| `yq` / `dasel` | YAML / TOML / XML парсеры |
| `xq` / `xmlstarlet` | XML |

---

## 🖥️ Система и железо

| Команда | Действие |
|---|---|
| `uname -a` | Информация о ядре |
| `lsb_release -a` / `cat /etc/os-release` | Дистрибутив |
| `lscpu` | Процессор |
| `lspci` | PCI-устройства |
| `lsusb` | USB-устройства |
| `free -h` | Память |
| `df -h` | Диски |
| `dmidecode` | DMI / SMBIOS (под root) |
| `hwinfo` / `inxi -F` | Подробная сводка железа |
| `neofetch` / `fastfetch` | Красивый вывод о системе |
| `dmesg` | Логи ядра |
| `journalctl -k` | Логи ядра (systemd) |
| `uptime` | Время работы / нагрузка |
| `date` | Текущая дата/время |
| `timedatectl` | Временная зона / NTP |

---

## 🔐 SSH и ключи

| Команда | Действие |
|---|---~|
| `ssh-keygen -t ed25519 -C "email"` | Создать пару ключей |
| `ssh-keygen -t rsa -b 4096` | RSA-ключ |
| `cat ~/.ssh/id_ed25519.pub` | Показать публичный ключ |
| `ssh-copy-id user@host` | Загрузить ключ на сервер |
| `ssh user@host` | Подключиться |
| `ssh -p 2222 user@host` | Нестандартный порт |
| `ssh -L 8080:localhost:80 user@host` | Порт-форвардинг (локальный) |
| `ssh -R 8080:localhost:80 user@host` | Обратный туннель |
| `ssh -D 1080 user@host` | SOCKS-прокси |
| `scp file user@host:/path` | Копирование файлов |
| `rsync -avz src/ user@host:dst/` | Синхронизация |

---

## ⌨️ Горячие клавиши в самом терминале (readline)

| Клавиша | Действие |
|---|---|
| `Ctrl-a` / `Ctrl-e` | В начало / конец строки |
| `Ctrl-b` / `Ctrl-f` | Влево / вправо на символ |
| `Alt-b` / `Alt-f` | Влево / вправо на слово |
| `Ctrl-d` | Удалить символ справа (= Del) |
| `Ctrl-h` | Удалить символ слева (= Backspace) |
| `Ctrl-w` | Удалить слово слева |
| `Ctrl-u` | Удалить до начала строки |
| `Ctrl-k` | Удалить до конца строки |
| `Ctrl-y` | Вставить удалённое (yank) |
| `Ctrl-r` | Обратный поиск по истории |
| `Ctrl-s` | Прямой поиск по истории |
| `Ctrl-l` | Очистить экран (= `clear`) |
| `Ctrl-c` | Прервать команду |
| `Ctrl-z` | Приостановить процесс |
| `Ctrl-d` на пустой строке | Выйти из shell (= `exit`) |
| `Tab` | Автодополнение |
| `↑` / `↓` | История команд |
| `!!` | Повторить последнюю команду |
| `!$` | Последний аргумент предыдущей команды |
| `Alt-.` | Последний аргумент (повтор — предыдущие) |

---

## 🚀 Современные альтернативы (когда скучно)

| Классика | Современная замена | Описание |
|---|---|---|
| `ls` | `eza` / `lsd` | с иконками и цветами |
| `cat` | `bat` | с подсветкой синтаксиса |
| `find` | `fd` | быстрее и удобнее |
| `grep` | `rg` (ripgrep) | быстрее |
| `top` | `htop`, `btop`, `glances` | красивее |
| `du` | `ncdu`, `dust` | интерактивно |
| `df` | `duf` | таблица с цветами |
| `cd` | `zoxide` (`z`) | переход по частоте |
| `tree` | `eza --tree` / `tre` | с иконками |
| `ps` | `procs` | читаемый вывод |
| `rm` | `trash-cli` | в корзину вместо удаления |
| `diff` | `delta` | красивые diff'ы |
| `git` | `lazygit`, `tig` | TUI |
| `man` | `tldr`, `tealdeer` | примеры вместо мануала |

Установить всё сразу (Arch):
```bash
sudo pacman -S eza bat fd ripgrep btop ncdu duf zoxide dust procs trash-cli git-delta lazygit tldr
```

---

## 🧰 Полезные мелочи

| Команда | Действие |
|---|---|
| `history \| grep foo` | История команд |
| `alias ll='ls -lah'` | Создать алиас (до перезагрузки) |
| `echo $SHELL` | Текущий shell |
| `chsh -s /usr/bin/zsh` | Сменить shell по умолчанию |
| `watch -n 1 <cmd>` | Запускать команду каждые 1с |
| `timeout 10 <cmd>` | Ограничить время выполнения |
| `yes \| <cmd>` | Авто-«yes» на вопросы |
| `command -v <cmd>` | Проверить, есть ли команда |
| `type <cmd>` | Что такое команда (alias/builtin/file) |
| `env` / `printenv` | Переменные окружения |
| `export VAR=value` | Установить переменную |
| `xclip -sel c < file` | Скопировать в буфер обмена |
| `wl-copy < file` | Буфер обмена Wayland |
| `date +%Y%m%d` | Дата в формате |
| `cal` / `ncal` | Календарь |
| `factor 12345` | Разложить на множители |
| `bc` | Калькулятор в терминале |

---

## 🔗 Полезные ссылки

- **tldr-pages:** https://tldr.sh — краткие примеры команд
- **explainshell:** https://explainshell.com — разбор сложных команд
- **cht.sh:** https://cht.sh — шпаргалки в терминале (`curl cht.sh/tar`)
- **The Art of Command Line:** https://github.com/jlevy/the-art-of-command-line
- **Linux Commands Cheat Sheet:** https://linuxcommandlibrary.com

---

## 💡 Полезные советы

1. **`man <cmd>`** — всегда под рукой; `/` для поиска, `q` — выход.
2. **`tldr <cmd>`** — если `man` слишком длинный, смотрите примеры.
3. **`Ctrl-r`** — обратный поиск по истории, лучшее, что есть в bash/zsh.
4. **`!!`** — повтор последней команды (например `sudo !!`).
5. **`cd -`** — прыгнуть туда-обратно между двумя каталогами.
6. **`pushd` / `popd`** — стек каталогов, удобно в скриптах.
7. **`tail -f /var/log/...`** — следить за логом в реальном времени.
8. **`xargs`** — передать вывод как аргументы, но осторожно с пробелами
   (используйте `-0` вместе с `find -print0`).
9. **Кавычки:** одинарные `'...'` — без подстановки, двойные `"..."` — с
   раскрытием переменных и команд, обратные `\`cmd\`` — то же, что `$(cmd)`.
10. **Не делайте `rm -rf /`** — это не шутка, проверяйте пути дважды.
11. **`chmod +x script.sh`** перед запуском скрипта `./script.sh`.
12. **Переменная `PATH`** — где shell ищет команды:
    `echo $PATH`, добавить: `export PATH="$PATH:/new/dir"`.

---

*Сгенерировано как шпаргалка. Команды могут отличаться в зависимости от
дистрибутива — проверяйте через `<cmd> --help` или `man <cmd>`.*
