# 📦 Yazi — шпаргалка по горячим клавишам

> Yazi — быстрый терминальный файловый менеджер на Rust.
> Полная справка внутри программы: `?`
> Дом: https://yazi-rs.github.io

---

## 📂 Навигация

| Клавиша | Действие |
|---|---|
| `k` / `↑` | Вверх |
| `j` / `↓` | Вниз |
| `K` | Вверх на 5 строк |
| `J` | Вниз на 5 строк |
| `g g` | В начало списка |
| `G` | В конец списка |
| `Ctrl-u` | На пол-страницы вверх |
| `Ctrl-d` | На пол-страницы вниз |
| `Ctrl-b` | На страницу вверх |
| `Ctrl-f` | На страницу вниз |
| `h` | Вверх по каталогу (в родительский) |
| `l` | Войти в каталог |
| `Enter` | Открыть файл / каталог |
| `o` | Открыть выбранным приложением |
| `~` | Перейти в домашний каталог |

---

## ✏️ Действия с файлами

| Клавиша | Действие |
|---|---|
| `Space` | Выделить / снять выделение |
| `v` | Визуальный режим (выделение) |
| `V` | Выйти из визуального режима |
| `y` | Копировать (yank) |
| `x` | Вырезать (cut) |
| `p` | Вставить |
| `P` | Вставить с перезаписью |
| `d` | Удалить в корзину (trash) |
| `D` | Удалить навсегда (без корзины) |
| `a` | Создать файл/папку |
| `r` | Переименовать |
| `o` | Открыть приложением по умолчанию |
| `O` | Открыть через… (выбор приложения) |
| `.` | Показать / скрыть скрытые файлы |

---

## 🔍 Поиск и фильтр

| Клавиша | Действие |
|---|---|
| `/` | Поиск по именам |
| `f` | Фильтр в текущем каталоге |
| `s` | Найти (fzf) |
| `S` | Найти (zoxide) |
| `z` | Сменить каталог через zoxide |
| `Z` | Сменить каталог (fzf) |

---

## 🗂️ Вкладки

| Клавиша | Действие |
|---|---|
| `t` | Новая вкладка |
| `1`–`9` | Перейти на вкладку №1–9 |
| `[` | Предыдущая вкладка |
| `]` | Следующая вкладка |
| `{` | Поменять местами с предыдущей |
| `}` | Поменять местами со следующей |

---

## 🔧 Сортировка (префикс `,`)

| Клавиша | Действие |
|---|---|
| `,m` | По дате изменения |
| `,b` | По дате создания |
| `,s` | По размеру |
| `,n` | По имени |
| `,c` | Случайно |
| `,d` | По каталогу |
| `,dir` | Каталоги сначала |

---

## 🖼️ Превью и режимы

| Клавиша | Действие |
|---|---|
| `;` | Включить / выключить превью |
| `Tab` | Переключить фокус между панелями |
| `M` | Открыть mount-менеджер |

---

## 🛠️ Прочее

| Клавиша | Действие |
|---|---|
| `:` | Командная строка |
| `?` | Справка (полный список биндингов для текущего контекста) |
| `q` | Выйти (с записью cwd в оболочку при наличии wrapper'а) |
| `Q` | Выйти без смены cwd |

---

## 🐚 Интеграция с оболочкой (shell wrapper `yy`)

Yazi умеет менять текущий каталог оболочки при выходе. Для этого нужно
добавить функцию-обёртку.

### Bash (`~/.bashrc`)
```bash
function yy() {
    local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
    yazi "$@" --cwd-file="$tmp"
    if cwd="$(command cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
        builtin cd -- "$cwd"
    fi
    rm -f -- "$tmp"
}
```

### Zsh (`~/.zshrc`)
```zsh
function yy() {
    local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
    yazi "$@" --cwd-file="$tmp"
    if cwd="$(command cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
        builtin cd -- "$cwd"
    fi
    rm -f -- "$tmp"
}
```

### Fish (`~/.config/fish/functions/y.fish`)
```fish
function y
    set tmp (mktemp -t "yazi-cwd.XXXXXX")
    yazi $argv --cwd-file="$tmp"
    read -z cwd < "$tmp"
    if test -n "$cwd" -a "$cwd" != "$PWD"
        builtin cd "$cwd"
    end
    rm -f "$tmp"
end
```

### Nushell (`~/.config/nushell/config.nu` или env.nu)
```nu
def --env yy [...args] {
    let tmp = (mktemp -t "yazi-cwd.XXXXXX")
    yazi ...$args --cwd-file $tmp
    let cwd = (open $tmp)
    if $cwd != "" and $cwd != $env.PWD {
        cd $cwd
    }
    rm -fp $tmp
}
```

После запускаете `yy` вместо `yazi` — и при выходе shell перейдёт в каталог,
где вы закончили.

---

## ⚙️ Конфигурация

Расположение файлов конфигурации: `~/.config/yazi/`

| Файл | Назначение |
|---|---|
| `yazi.toml` | Основные настройки (поведение, плагины, opener'ы) |
| `keymap.toml` | Горячие клавиши (переопределение биндингов) |
| `theme.toml` | Тема оформления / цвета |
| `init.lua` | Lua-инициализация (хуки, кастомные команды) |

Пример своего биндинга в `keymap.toml`:
```toml
[manager]
prepend_keymap = [
    # открыть выделенное в Neovim
    { on = [ "T" ], run = "shell 'nvim \"$@\"' --confirm", desc = "Open in nvim" },
    # открыть выделенное в VS Code
    { on = [ "C" ], run = "shell 'code \"$@\"' --confirm", desc = "Open in VS Code" },
]
```

---

## 💡 Полезные советы

1. **`?`** — ваш главный помощник: показывает биндинги для текущего контекста
   (manager, tasks, select, input, help).
2. **Массовые действия**: выделите файлы через `Space`, затем `y`/`x`/`d`/`p`.
3. **Быстрый переход**: `z` (zoxide) по частотным каталогам.
4. **Превью**: yazi умеет показывать изображения, PDF, видео-кадры, исходники
   с подсветкой. Нужны `chafa`/`ueberzugpp` для картинок, `ffmpeg` для видео.
5. **Плагины**: см. https://yazi-rs.github.io/docs/plugins
6. **Обновление темы/конфига на лету**: внутри yazi `:ya sync` (или просто
   перезапустите).
7. **Открыть терминал в текущем каталоге**: добавьте в `keymap.toml`
   биндинг `shell '$SHELL' --block --confirm`.

---

*Сгенерировано как шпаргалка. Версия может отличаться — проверяйте через `?` в yazi.*
